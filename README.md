# 机场机位分配与拖曳调度 Demo

React + Vite 单页演示应用，包含三栏布局：

- 左侧：自动生成的航班列表
- 中间：机位占用与拖曳任务时间轴
- 右侧：拖车资源、电量、任务负载与可用状态

## 运行

```bash
npm install
npm run dev
```

当前 Codex shell 环境可用 `node`，但未检测到 `npm` / `pnpm` / `yarn`，所以本次未能在本机安装依赖并启动预览。项目文件已按标准 Vite 结构创建。
